<a href="{{ path('add_classroom') }}">Add Student</a>
<h4>List Student</h4>
{% for s in  tab_class %}
    nce: {{ s.id }}
    username: {{ s.name }}
    moyenne: {{ s.Description }}
    <a href="{{ path('update_classroom',{'nce':s.id}) }}">Update</a>
    <a href="{{ path('remove',{'nce':s.id}) }}">Remove</a>
    <br>
{% endfor %}