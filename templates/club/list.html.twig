<img src="{{ asset('img/fugu1.jpg') }}" alt="student">
<h1>La  liste de formations de la classe {{c1}} dans la salle {{c2}}</h1>
{% if tab_formations is empty %}
<h6 style="color: coral">Pas de formations</h6>
{% else %}
{% for i in tab_formations %}
    {% if i.Description == "theorique"%}
    ref: {{i.ref}} titre: {{i.Titre|upper}} description: {{i.Description}} nb participants
    {{i.nb_participants}}
    Participer :
    {% if i.nb_participants == 0 %}<a href="{{ path('app_reservation') }}">Participer</a>
    {% else %}
    {% endif %}
        complet
        
    {% endif %}
    
    <br>

   
{% endfor %}
{% endif %} 
{% set somme = 0 %}
            {% for j in tab_formations %}
       
                     {% set  somme = somme + j.nb_participants %}
                    
                      
            {% endfor %}   somme = {{somme}}
{% set max_value= 0 %}

{% for i in tab_formations %}
    {% set max_value = max(i.nb_participants,max_value) %} 
 {% endfor %}
 <br>
 max participants={{ max_value }}

<h5 style="color: blue">Nbr de formations: {{ tab_formations|length }}</h5>
{% set x =10 %}
x={{ x }}
